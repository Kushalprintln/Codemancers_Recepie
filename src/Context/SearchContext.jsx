import { createContext } from "react";

// Search Context for searching food item
// using in Main section of the page for data flow;

const SearchContext = createContext();

export default SearchContext;